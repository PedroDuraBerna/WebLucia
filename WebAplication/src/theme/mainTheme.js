
import { createTheme } from "@mui/material";
import { purpleTheme } from "./themes";

// Este código crea el tema que usaremos en el componente AppTheme.jsx
export const mainTheme = createTheme(purpleTheme); 